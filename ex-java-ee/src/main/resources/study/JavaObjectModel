java对象在内存中的存储布局：
    普通对象：
        markword
        类型指针        前两个加一块叫对象头
        实例数据
        对齐填充

    数组：
        mark word
        类型指针    (class printer) 前两个加一块是对象头
        数组长度    (length)
        实例数据    (instance data)
        对齐填充    (padding)