数据结构：
    线性结构：   一维数组，链表，队列，栈
    非线性结构：  多维数组，广义表，图，树。

用二维数组映射一个五子棋棋盘，但大部分场景二维数组都是不满的，值为0，这里用到稀疏数组。sparsearray
当一个数组中大部分元素都为0，或者为同一个值的数组时，可以使用稀疏数组来保存该数组，
稀疏数组的处理方法：
    记录数组一共有几列几行，有多少个不同的值。把具有不同值的元素的行列及值记录在一个小规模的数组中，从而缩小程序的规模。