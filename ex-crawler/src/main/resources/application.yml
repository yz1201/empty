# DB Configuration:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///db_crawler?useSSL=false&useUnicode=true&characterEncoding=UTF8&serverTimezone=GMT&allowPublicKeyRetrieval=true
    username: root
    password: root
  #JPA Configuration:
  jpa:
    database: mysql
    show-sql: true
  application:
    name: crawler
server:
  port: 12100
